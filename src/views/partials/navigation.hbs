<nav class="nav-container">
    <h3 class="larger--all">Categories</h3>
    <ul class="box-categories">
        {{#each userData.categories}}
            <li class="box-categories__item">
                <button class="categories--btn" href="{{url}}">{{name}}</button>
                <input class="done clear" type="submit" value="">
            </li>
        {{/each}}
    </ul>
    {{> themeswitch}}
</nav>