<nav class="nav-container">
    <h3 class="larger--all">Categories</h3>
    <ul class="box-categories">
        {{#each categoryData}}
            <li class="box-categories__item">
                <button class="categories--btn" href="{{slug}}">{{#capital title}}{{/capital}}</button>
                <form class="input__form" method="POST">
                    <input type="hidden" name="id" value="{{id}}">
                    <button formaction="/deleteCategory" type="submit" class="done clear"></button>
                </form>      
            </li>
        {{/each}}
    </ul>
</nav>